import 'package:flutter/material.dart';

class TabOtherPage extends StatelessWidget {
  const TabOtherPage({super.key});

  static const routeName = '/tab_other';

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Outra Página')));
  }
}
